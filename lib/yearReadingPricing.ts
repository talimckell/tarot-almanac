// Single source of truth for the paid year reading's price. Checkout, the sample page's
// CTA, and any marketing copy read from here, so changing the price is a one-line edit
// (plus the matching Stripe Price object / STRIPE_PRICE_ID_YEAR_READING env var).
export const YEAR_READING_PRICE_USD = 15;
export const YEAR_READING_PRICE_DISPLAY = "$15";
